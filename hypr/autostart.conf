#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

exec-once = systemctl --user start hyprpolkitagent
exec-once = gnome-keyring-daemon --start --components=secrets
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = awww-daemon & awww img /home/rainy/Pictures/Nord-Topographic.png
# exec-once = hyprctl output create headless Fake-Display
exec-once = bash -c 'while true; do waybar; sleep 1; done'
exec-once = hyprctl setcursor miku-cursor-linux 32
exec-once = easyeffects
exec-once = pwsp-daemon
exec-once = discord
